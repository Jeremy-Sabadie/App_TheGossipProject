<h1>THP Gossips</h1>

<% @gossips.each do |gossip| %>

  <h2><%= gossip.anonymous_author %></h2>
  <p><%= gossip.content %></p>
  <p><a href="<%= gossip_path(gossip.id) %>" class="btn btn-primary">Regarde moi ça</a></p>

<% end %>

<p><a href="<%= new_gossip_path %>" class="btn btn-default">Balance ton ragot</a></p>

